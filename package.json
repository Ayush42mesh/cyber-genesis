{
  "name": "cyber-genesis",
  "version": "1.0.0",
  "description": "CYBER GENESIS - Real-time Tech Fest Quiz Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --prefix client --include=dev && npm run build --prefix client",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
